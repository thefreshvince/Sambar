window.sambar = {

    // Holds the vue instance
    vue: null,

    // General configurations
    config: {

        // Sets the duration of the loading/page transition
        loading_transition_duration: 900

    },

    // Holds routes to add if routes are fetched before pageload
    routes_to_add: []

};